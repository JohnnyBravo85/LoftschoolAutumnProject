.socials {
  display: flex;
}

.socials__item {
  &:hover .socials__icon {
    fill: $hover-color;
  }
}

.socials__icon {
  height: 25px;
  fill: $social-icons-color;
  transition: fill ease-in-out 0.5s;
}